server:
  port: 8080

spring:
  datasource:
    upl: jdbc:postgresql://localhost:5432/postgres
    username: springnika
    password: 692652
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet

